#include<iostream>
using namespace std;
class Bank
{
    private:
    int an;
    char name[30];
    long balance;
    public:
    void getBank()
    {
        cout<<"Enter Acount Number:";
        cin>>an;
         cout<<"Enter Acount Holder Name:";
        cin>>name;
         cout<<"Enter Acount Balance:";
        cin>>balance;
        
    }
};
int Banksearch(int an)
{
    if(acno==an)
    {
        showAccount();
        return(1);
        return(0);
    }
    int main()
    {
        Bank C[3];
        int i;
        for(i=0; i<=2; i++)
        {
            C[i].openAccount();
        }
        cout<<"Main Menu\n1]shown All Account\n2]show by account\n3]Deposit\n4]";
        cout<<"Your Choice";
        cin>>ch;
        switch(ch)
        {
            case 1:
            for(i=0; i<=2; i++)
            {
                found=C[i].search(an);
                if(found)
                {
                    break;
                }
            fun.employee()
            {
                createFolder()
                {
                    main.c;
                    if(choice==1)
                    {
                        cout<<"Main Menu"<<mn<<endl;

                    }
                    else
                    {
                        {
                            cout<<"Exit"<<endl;
                        }
                    }
                    int a[3]={12,34,654,17,86};
                    {
                        found==!=1
                        cin>>not;
                        cout<<
                    }

                 double,int,arr[12,34,75,96];
                 if(c==1)   
                }
            }
            get(1)

            }
            char name.c[bank.Employee];
           
            employee1.Bank()
             employee2.Bank()
              employee3.Bank()
        }
        cout<<"Print all above options and displayed"
    }
    else
    {
        cout<<"Otherwise ignore all the options and do not displayed all above options"
    }
    getchar()
}